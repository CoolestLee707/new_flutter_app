import 'package:flutter/material.dart';

class WeChatSecond extends StatelessWidget {
  const WeChatSecond({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('发现'),
        elevation: 0.0,
      ),
      body: Text('2'),
    );
  }
}
