import 'package:flutter/material.dart';

class WeChatFour extends StatelessWidget {
  const WeChatFour({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('通讯录'),
        elevation: 0.0,
      ),
      body: Text('4'),
    );
  }
}
