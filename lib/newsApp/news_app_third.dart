import 'package:flutter/material.dart';

class NewsThird extends StatelessWidget {
  const NewsThird({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('电影'),
        elevation: 0.0,
        backgroundColor: Colors.red,
      ),
      body: Text('33'),
    );
  }
}
