import 'package:flutter/material.dart';

class NewsSecond extends StatelessWidget {
  const NewsSecond({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('视频'),
        elevation: 0.0,
        backgroundColor: Colors.red,
      ),
      body: Text('22'),
    );
  }
}
